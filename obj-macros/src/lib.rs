//! Procedural macros for the [`obj`](https://docs.rs/obj) crate.
//!
//! These are re-exported by `obj`; depend on that crate rather than this one.

// Implementations land in P2 onwards; this crate exists from P0 so the workspace, CI and
// dependency graph are wired up from the start.
